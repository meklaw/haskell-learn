module TupleDemo where

-- кортеж пара
-- (2, True)
-- разное кол-во элементов
-- (2, True, 'c')
-- fst (2, True) -- вернёт 2
-- snd (2, True) -- вернёт True
-- (2, True) :: (Num, Bool)
-- нет единичных кортежей, но есть пустые кортежи (), () :: ()
